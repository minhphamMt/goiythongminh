services:
  - type: web
    name: embedding-service
    runtime: python
    rootDir: embedding_service
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.render_service
    plan: starter
    numInstances: 1
    healthCheckPath: /health
    envVars:
      - key: EMBEDDING_API_TOKEN
        sync: false
